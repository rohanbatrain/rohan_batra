import { describe, it } from 'vitest';

describe('Integration: Courses index', () => {
  it('filters by difficulty and tags, sorts and paginates', async () => {
    // TODO: seed stub and validate via listPublicCourses with params
  });
});
