import 'package:flutter/material.dart';

class SucklessDmenuScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Suckless Dmenu'),
      ),
      body: Center(
        child: Text('Details for Suckless Dmenu repository'),
      ),
    );
  }
}