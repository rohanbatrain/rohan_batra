import 'package:flutter/material.dart';

class SucklessStScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Suckless St'),
      ),
      body: Center(
        child: Text('Details for Suckless St repository'),
      ),
    );
  }
}