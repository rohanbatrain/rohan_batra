import 'package:flutter/material.dart';

class SucklessDwmScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Suckless Dwm'),
      ),
      body: Center(
        child: Text('Details for Suckless Dwm repository'),
      ),
    );
  }
}