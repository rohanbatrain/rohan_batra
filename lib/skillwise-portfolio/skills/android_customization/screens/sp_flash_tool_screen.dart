import 'package:flutter/material.dart';

class SpFlashToolScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('SP Flash Tool'),
      ),
      body: const Center(
        child: Text('Details about SP Flash Tool'),
      ),
    );
  }
}
