import 'package:flutter/material.dart';

class FastbootScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Fastboot'),
      ),
      body: const Center(
        child: Text('Details about Fastboot'),
      ),
    );
  }
}
