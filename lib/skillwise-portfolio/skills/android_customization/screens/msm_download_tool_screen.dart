import 'package:flutter/material.dart';

class MsmDownloadToolScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('MSM Download Tool'),
      ),
      body: const Center(
        child: Text('Details about MSM Download Tool'),
      ),
    );
  }
}
