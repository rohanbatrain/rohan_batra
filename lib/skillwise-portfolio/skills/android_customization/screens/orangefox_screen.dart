import 'package:flutter/material.dart';

class OrangeFoxScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('OrangeFox'),
      ),
      body: const Center(
        child: Text('Details about OrangeFox'),
      ),
    );
  }
}
