import 'package:flutter/material.dart';

class MagiskScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Magisk'),
      ),
      body: const Center(
        child: Text('Details about Magisk'),
      ),
    );
  }
}
