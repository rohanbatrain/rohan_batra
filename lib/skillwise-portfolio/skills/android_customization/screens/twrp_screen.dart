import 'package:flutter/material.dart';

class TwrpScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('TWRP'),
      ),
      body: const Center(
        child: Text('Details about TWRP'),
      ),
    );
  }
}
