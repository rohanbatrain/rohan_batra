import 'package:flutter/material.dart';

class VirtualizationContainersScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Virtualization – Containers (Pihole)'),
      ),
      body: Center(
        child: const Text('Content for Virtualization – Containers (Pihole)'),
      ),
    );
  }
}
