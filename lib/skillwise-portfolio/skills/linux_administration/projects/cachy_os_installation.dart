import 'package:flutter/material.dart';

class CachyOsInstallation extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Cachy OS Installation'),
      ),
      body: Padding(
        padding: const EdgeInsets.all(16.0),
        child: Text(
          'Installation experience on an HP Pavilion 360 Convertible, demonstrating versatility with different hardware.\n\nSource: Cachy OS Installation Guide',
        ),
      ),
    );
  }
}
