import 'package:flutter/material.dart';

class LinuxAdministrationScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Linux Administration')),
      body: Center(child: Text('Details about Linux Administration')),
    );
  }
}
