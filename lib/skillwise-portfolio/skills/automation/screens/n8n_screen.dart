import 'package:flutter/material.dart';

class N8nScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('n8n'),
      ),
      body: const Center(
        child: Text('Details about n8n'),
      ),
    );
  }
}
