import 'package:flutter/material.dart';

class IftttScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('IFTTT'),
      ),
      body: const Center(
        child: Text('Details about IFTTT'),
      ),
    );
  }
}
