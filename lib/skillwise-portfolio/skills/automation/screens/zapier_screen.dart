import 'package:flutter/material.dart';

class ZapierScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Zapier'),
      ),
      body: const Center(
        child: Text('Details about Zapier'),
      ),
    );
  }
}
