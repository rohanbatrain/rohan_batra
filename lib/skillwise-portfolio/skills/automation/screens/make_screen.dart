import 'package:flutter/material.dart';

class MakeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Make'),
      ),
      body: const Center(
        child: Text('Details about Make'),
      ),
    );
  }
}
