import 'package:flutter/material.dart';

class AnsibleScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Ansible'),
      ),
      body: const Center(
        child: Text('Details about Ansible'),
      ),
    );
  }
}
