import 'package:flutter/material.dart';

class FullStackDevelopmentScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Full-Stack Development')),
      body: Center(child: Text('Details about Full-Stack Development')),
    );
  }
}